---
phase: 01-foundation-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/requirements.txt
  - src/config/__init__.py
  - src/config/settings.py
  - src/config/loader.py
  - src/monitor.py
  - tests/conftest.py
  - tests/test_config.py
  - .env.example
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "Running `python src/monitor.py --env staging` loads staging targets (waldoclick.dev URLs)"
    - "Running `python src/monitor.py --env prod` loads prod targets (waldo.click URLs)"
    - "Missing CLOUDFLARE_API_TOKEN causes immediate exit with clear error"
    - "Missing MAILGUN_API_KEY causes immediate exit with clear error"
    - "API tokens are read from environment variables, not hardcoded"
  artifacts:
    - path: "src/config/__init__.py"
      provides: "Config class export"
      exports: ["Config"]
    - path: "src/config/settings.py"
      provides: "Environment-specific settings"
      contains: "ENVIRONMENTS"
    - path: "src/config/loader.py"
      provides: "Secret loading with validation"
      contains: "load_dotenv"
    - path: "src/monitor.py"
      provides: "CLI entry point with --env flag"
      contains: "argparse"
    - path: "tests/test_config.py"
      provides: "Config module tests"
      min_lines: 50
  key_links:
    - from: "src/monitor.py"
      to: "src/config/__init__.py"
      via: "Config.load(env_name)"
      pattern: "Config\\.load"
    - from: "src/config/__init__.py"
      to: "src/config/loader.py"
      via: "load_secrets()"
      pattern: "load_secrets"
    - from: "src/config/loader.py"
      to: ".env"
      via: "load_dotenv()"
      pattern: "load_dotenv"
---

<objective>
Create environment-aware configuration module and CLI entry point for waldo-shield monitor.

Purpose: Foundation for all subsequent phases - config loading must work before any data collection or reporting.

Output: 
- Config module that loads staging/prod-specific targets, recipients, and API tokens
- monitor.py entry point with --env flag
- Test suite covering config loading and secret validation
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-config/01-RESEARCH.md
@src/scanner.py
</context>

<interfaces>
<!-- From existing codebase - patterns to follow -->

From src/scanner.py:
```python
# CLI pattern to follow
parser = argparse.ArgumentParser(
    description="...",
    formatter_class=argparse.RawDescriptionHelpFormatter,
)
parser.add_argument("--output", "-o", default=None, help="...")

# Module structure pattern
from modules import headers, ssl_tls, dns_analysis
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test infrastructure and config module skeleton</name>
  <files>
    src/requirements.txt
    src/config/__init__.py
    src/config/settings.py
    src/config/loader.py
    tests/conftest.py
    tests/test_config.py
  </files>
  <behavior>
    - test_staging_config: Config.load("staging") returns config with waldoclick.dev targets
    - test_prod_config: Config.load("prod") returns config with waldo.click targets
    - test_invalid_env: Config.load("invalid") raises ValueError
    - test_secrets_from_env: Config.load() reads CLOUDFLARE_API_TOKEN from os.environ
    - test_missing_secret_error: Missing required env var raises EnvironmentError with clear message
  </behavior>
  <action>
1. Update src/requirements.txt - add python-dotenv>=1.2.2 and pytest>=8.0.0

2. Create src/config/__init__.py:
   - Import and re-export Config class from loader
   - Keep this file minimal (just exports)

3. Create src/config/settings.py:
   - Define EnvironmentConfig dataclass (frozen=True) with: name, targets, recipients, mailgun_domain
   - Define ENVIRONMENTS dict with "staging" and "prod" entries:
     - staging: waldoclick.dev URLs, security@waldoclick.dev recipient
     - prod: waldo.click URLs, security@waldo.click recipient
   - Function get_env_settings(env_name) that returns settings or raises ValueError

4. Create src/config/loader.py:
   - Import dataclasses, os, pathlib, dotenv
   - Define Config dataclass (frozen=True) with: environment, targets, recipients, cloudflare_token, mailgun_api_key, mailgun_domain
   - load_secrets() function that:
     - Calls load_dotenv() with explicit path to project root .env
     - Reads CLOUDFLARE_API_TOKEN and MAILGUN_API_KEY from os.environ
     - Raises EnvironmentError if either missing with clear message listing missing vars
     - Returns dict with tokens
   - Config.load(env_name) classmethod that combines settings + secrets into Config instance

5. Create tests/conftest.py:
   - Fixture `clean_env` that clears config-related env vars before/after test
   - Fixture `mock_secrets` that sets CLOUDFLARE_API_TOKEN and MAILGUN_API_KEY to test values

6. Create tests/test_config.py:
   - Import pytest, Config
   - test_staging_config: verify targets contain waldoclick.dev
   - test_prod_config: verify targets contain waldo.click  
   - test_invalid_env: verify ValueError raised
   - test_secrets_from_env: verify tokens loaded from env
   - test_missing_secret_error: verify EnvironmentError with clear message

Follow patterns from 01-RESEARCH.md code examples. Use frozen=True dataclasses.
  </action>
  <verify>
    <automated>cd src && pip install -q python-dotenv pytest && cd .. && pytest tests/test_config.py -v</automated>
  </verify>
  <done>
    - All 5 tests pass
    - Config.load("staging") returns staging config
    - Config.load("prod") returns prod config
    - Missing secrets cause EnvironmentError
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI entry point (monitor.py)</name>
  <files>
    src/monitor.py
    .env.example
  </files>
  <action>
1. Create src/monitor.py following scanner.py patterns:
   - Shebang: #!/usr/bin/env python3
   - Docstring explaining usage
   - Import argparse, sys
   - Import Config from config module
   - parse_args() function:
     - Create ArgumentParser with description
     - Add --env/-e argument: choices=["staging", "prod"], required=True
     - Add --dry-run flag: action="store_true"
     - Return parsed args
   - main() function:
     - Parse args
     - Try to load config: config = Config.load(args.env)
     - Print confirmation: f"Loaded {config.environment} config with {len(config.targets)} targets"
     - Print targets list
     - If dry_run, print "(dry run - no actions taken)"
     - Exit 0
   - Handle EnvironmentError gracefully: print error message, exit 1
   - if __name__ == "__main__": main()

2. Create .env.example with placeholder values:
   ```
   # Cloudflare API token (API Tokens, not Global API Key)
   CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
   
   # Mailgun API key
   MAILGUN_API_KEY=your_mailgun_api_key_here
   ```

Do NOT create actual .env file (user must create from .env.example).
  </action>
  <verify>
    <automated>CLOUDFLARE_API_TOKEN=test MAILGUN_API_KEY=test python src/monitor.py --env staging --dry-run</automated>
  </verify>
  <done>
    - Running with --env staging prints "Loaded staging config with 3 targets"
    - Running with --env prod prints "Loaded prod config with 3 targets"
    - Running without required env vars prints clear error and exits 1
    - .env.example exists with placeholder values
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration verification and cleanup</name>
  <files>
    src/config/__init__.py
  </files>
  <action>
1. Run full test suite to verify everything works together
2. Verify monitor.py fails gracefully without .env:
   - Unset env vars, run monitor.py, confirm EnvironmentError message
3. Verify .gitignore includes .env (should already be there based on existing project)
4. Add __all__ export to src/config/__init__.py if not already present

Final verification commands:
- pytest tests/test_config.py -v (all tests pass)
- python src/monitor.py --env staging --help (shows help)
- Without env vars: python src/monitor.py --env staging (shows missing vars error)
- With env vars: CLOUDFLARE_API_TOKEN=x MAILGUN_API_KEY=y python src/monitor.py --env prod --dry-run
  </action>
  <verify>
    <automated>pytest tests/test_config.py -v && CLOUDFLARE_API_TOKEN=test MAILGUN_API_KEY=test python src/monitor.py --env prod --dry-run</automated>
  </verify>
  <done>
    - Full test suite passes
    - CLI works with both staging and prod
    - Missing env vars produce clear error
    - .env.example exists for user reference
  </done>
</task>

</tasks>

<verification>
Phase 1 verification checklist:
1. `pytest tests/test_config.py -v` - all config tests pass
2. `python src/monitor.py --help` - shows usage with --env flag
3. `CLOUDFLARE_API_TOKEN=x MAILGUN_API_KEY=y python src/monitor.py --env staging --dry-run` - loads staging config
4. `CLOUDFLARE_API_TOKEN=x MAILGUN_API_KEY=y python src/monitor.py --env prod --dry-run` - loads prod config
5. `python src/monitor.py --env staging` (without env vars) - shows missing env var error
</verification>

<success_criteria>
- Config module loads environment-specific settings (CONF-01)
- API tokens read from environment variables (CONF-02)
- Clear error messages when secrets missing
- Test coverage for all config behaviors
- CLI entry point works with --env flag
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-config/01-01-SUMMARY.md`
</output>
