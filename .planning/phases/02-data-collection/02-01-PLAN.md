---
phase: 02-data-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/requirements.txt
  - src/modules/email_auth.py
  - tests/test_email_auth.py
  - tests/conftest.py
autonomous: true
requirements: [DNS-01, DNS-02, DNS-03, DNS-04]

must_haves:
  truths:
    - "Scanner outputs SPF validation results for waldo.click and waldoclick.dev"
    - "Scanner outputs DKIM validation results for both domains"
    - "Scanner outputs DMARC policy validation with effectiveness warnings"
    - "Scanner outputs CAA validation showing expected CA (pki.goog)"
  artifacts:
    - path: "src/modules/email_auth.py"
      provides: "Email authentication validation (SPF, DKIM, DMARC, CAA)"
      exports: ["check_email_security", "check_caa_records"]
      min_lines: 80
    - path: "tests/test_email_auth.py"
      provides: "Unit tests for email authentication checks"
      min_lines: 60
  key_links:
    - from: "src/modules/email_auth.py"
      to: "checkdmarc library"
      via: "checkdmarc.check_domains()"
      pattern: "checkdmarc\\.check_domains"
    - from: "src/modules/email_auth.py"
      to: "dns.resolver"
      via: "CAA record query"
      pattern: "dns\\.resolver\\.resolve.*CAA"
---

<objective>
Create comprehensive DNS/email authentication module using checkdmarc library for SPF, DKIM, DMARC validation with proper lookup counting and policy effectiveness warnings, plus CAA validation against expected CA (pki.goog).

Purpose: Replace basic pattern-matching in existing dns_analysis.py with industry-standard validation that catches edge cases (SPF 10-lookup limit, ineffective DMARC policies, etc.)

Output: src/modules/email_auth.py module with tests, ready for Phase 3 report integration
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-collection/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 -->

From src/config/loader.py:
```python
@dataclass(frozen=True)
class Config:
    environment: str
    targets: List[str]
    recipients: List[str]
    cloudflare_token: str
    mailgun_api_key: str
    mailgun_domain: str

    @classmethod
    def load(cls, env_name: str) -> "Config":
        """Load configuration for specified environment."""
```

From src/config/settings.py:
```python
ENVIRONMENTS = {
    "staging": EnvironmentConfig(
        name="staging",
        targets=["https://api.waldoclick.dev", "https://dashboard.waldoclick.dev", "https://www.waldoclick.dev"],
        mailgun_domain="waldoclick.dev",
    ),
    "prod": EnvironmentConfig(
        name="prod", 
        targets=["https://api.waldo.click", "https://dashboard.waldo.click", "https://www.waldo.click"],
        mailgun_domain="waldo.click",
    ),
}
```

<!-- Apex domains to check: waldoclick.dev (staging), waldo.click (prod) -->
<!-- Expected CA: pki.goog (Google Trust Services - per CLAUDE.md) -->
<!-- DKIM selectors: Likely from Mailgun - check common selectors (mx, smtp, mailgun) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install checkdmarc and create email_auth module with tests</name>
  <files>src/requirements.txt, src/modules/email_auth.py, tests/test_email_auth.py, tests/conftest.py</files>
  <behavior>
    - Test: check_email_security("waldo.click") returns dict with spf, dkim, dmarc keys
    - Test: SPF result includes "valid" boolean and any lookup count warnings
    - Test: DMARC result includes policy (p=) and effectiveness warnings for p=none
    - Test: DKIM result attempts validation with known selectors
    - Test: check_caa_records("waldo.click", "pki.goog") returns valid=True when CA present
    - Test: check_caa_records returns valid=False with issue when CA missing
    - Test: Functions handle DNS timeout gracefully (return error, don't crash)
  </behavior>
  <action>
1. Add checkdmarc>=5.13.4 to src/requirements.txt (after existing entries)

2. Create src/modules/email_auth.py with:
   - `check_email_security(domain: str, dkim_selectors: list = None) -> dict`
     - Uses checkdmarc.check_domains() with skip_tls=True, timeout=5.0
     - Extracts apex domain from subdomain input (api.waldoclick.dev → waldoclick.dev)
     - Returns structured dict: {domain, spf: {...}, dkim: {...}, dmarc: {...}, issues: [...]}
     - Converts checkdmarc warnings to issues with severity levels
   - `check_caa_records(domain: str, expected_ca: str = "pki.goog") -> dict`
     - Uses dns.resolver.resolve(domain, "CAA")
     - Checks if expected_ca appears in any CAA record
     - Returns: {records: [...], expected_ca, valid: bool, issues: [...]}
   - `analyze_domain(domain: str) -> dict`
     - Combines email_security + CAA check
     - Single entry point for Phase 3 report integration

3. Add to tests/conftest.py:
   - `mock_checkdmarc` fixture that patches checkdmarc.check_domains
   - `mock_dns_caa` fixture that patches dns.resolver.resolve for CAA

4. Create tests/test_email_auth.py with tests covering all behaviors above.
   Use mocked responses - no live DNS queries in tests.

Note: Use existing dnspython (already in requirements) for CAA - checkdmarc doesn't include CAA.
Default DKIM selectors to check: ["default", "mailgun", "mx", "smtp", "k1"] per research.
  </action>
  <verify>
    <automated>pip install -r src/requirements.txt && pytest tests/test_email_auth.py -v</automated>
  </verify>
  <done>
    - checkdmarc added to requirements
    - email_auth.py exports check_email_security, check_caa_records, analyze_domain
    - All tests pass with mocked DNS responses
    - Functions return structured dicts matching expected schema
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify module works with real DNS queries</name>
  <files>src/modules/email_auth.py</files>
  <action>
Create a manual verification script (don't commit) that:
1. Imports email_auth module
2. Calls analyze_domain("waldo.click")
3. Calls analyze_domain("waldoclick.dev")
4. Prints results in readable format

Run it and verify:
- SPF records found for both domains
- DMARC records found with policy extracted
- CAA shows pki.goog as expected CA
- No exceptions or crashes

If DKIM validation fails (selectors unknown), note in summary - not a blocker, can be configured later.

Fix any issues found during real DNS testing.
  </action>
  <verify>
    <automated>python -c "from src.modules.email_auth import analyze_domain; r = analyze_domain('waldo.click'); assert 'spf' in r and 'dmarc' in r and 'caa' in r, 'Missing required keys'"</automated>
  </verify>
  <done>
    - analyze_domain returns valid results for real domains
    - SPF and DMARC data extracted correctly
    - CAA validation identifies pki.goog
    - Error handling works for edge cases
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pytest tests/test_email_auth.py -v` — all tests pass
2. `python -c "from src.modules.email_auth import analyze_domain; print(analyze_domain('waldo.click'))"` — returns structured result
3. No imports from old dns_analysis.py email functions (this module replaces them for email auth)
</verification>

<success_criteria>
- checkdmarc library installed and working
- email_auth.py provides comprehensive SPF/DKIM/DMARC/CAA validation
- Tests use mocked DNS to run fast and reliably
- Real domain queries work without errors
- Ready for Phase 3 report integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-collection/02-01-SUMMARY.md`
</output>
